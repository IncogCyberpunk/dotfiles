# --- Font Settings ---
font-family = "JetBrainsMono NF"
# 700 weight corresponds to Bold
font-style = "Bold Italic"
# Fallback font
font-family = "Lohit Nepali"
font-size = 11.5

# --- Visuals ---
theme = Catppuccin Mocha

# --- Window Geometry ---
window-width = 120
window-height = 28
# Ghostty hides the tab bar by default if only one tab exists
# but you can ensure native decorations are handled:
window-decoration = true

# --- Keybindings ---
keybind = alt+enter=unbind

confirm-close-surface = false
