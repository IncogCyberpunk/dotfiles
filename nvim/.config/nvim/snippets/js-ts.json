{
  "console log":{
    "prefix":"cl",
    "body": [
      "console.log(`$1`);"
    ]
  },
  "for loop":{
    "prefix":"fo",
    "body":[
      "for (let i=${1:0}; i < ${2:condition}; ${3:i++}) {",
      "$4",
      "}"
    ]
  }
}
